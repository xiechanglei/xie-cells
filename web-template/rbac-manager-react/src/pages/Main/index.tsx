import {Button, HStack} from "@chakra-ui/react"

/**
 * 用户登陆之后的程序主框架，框架使用分栏设计，
 * 顶部是固定高度的一级导航栏，导航栏内部左侧是logo，后面是导航栏，右侧是功能搜索以及切换主题，语言，用户信息，通知消息功能
 * @constructor
 */

const Main = () => {
    return
}

export default Main